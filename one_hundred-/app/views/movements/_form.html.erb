<%= simple_form_for([@workout, @workout.movements.build]) do |f| %>
  <div class="form-group">
    <select class="form-control">
      <option> <%= options_from_collection_for_select(Movement.all, :movement, :movement) %> </option>
   </select>
 </div>
  <%= f.input :reps, input_html: { class: "form-control"} %>
  <br>
  <%= f.button :submit %>

<% end %>
