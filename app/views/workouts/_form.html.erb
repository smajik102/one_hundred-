<%= simple_form_for(@workout, html: {class: 'form-horizontal' }) do |f| %>
  <%= f.input :date, label: "Date" %>
  <%= f.input :workout, label: "WOD", input_html: { class: "form-control"} %>
  <%= f.input :notes, label: "Coaches Notes", input_html: { class: "form-control"} %>
  <br>
  <%= f.button :submit %>

  <% end %>

<h1> test </h1>
